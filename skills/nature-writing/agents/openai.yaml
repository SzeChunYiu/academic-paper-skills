interface:
  display_name: "Nature Writing (Legacy)"
  short_description: "Legacy writing implementation; use academic-writing"
  default_prompt: "Use $nature-writing only for backward-compatible legacy writing workflows; prefer $academic-writing for new academic manuscript work."
policy:
  allow_implicit_invocation: false
